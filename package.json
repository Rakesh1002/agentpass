{
  "name": "agentpass",
  "version": "0.1.0",
  "description": "Credential broker for AI agents — API keys, passwords, secrets",
  "type": "module",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "bin": {
    "agentpass": "./src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "test": "bun test",
    "typecheck": "bun x tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node-forge": "^1.3.14",
    "typescript": "^5"
  },
  "dependencies": {
    "node-forge": "^1.4.0"
  }
}
